# 1. Create an array to store 3 words. Then add two more words to the array and print the array on one line.

# Need a new array,
# with 3 words already in it

# then we will put two more words in
# then return the 5 word array

# I want my function to take in a string of words, seperated by commas / to check for commas, and spaces, and seperate into string words
# Then i want those seperated strings, stored as an array, to be joined with the current array

# def word_seperate(n)
#   original_array = ["Fizz", "Buzz", "FizzBuz"]
#   p original_array
#   original_array += n.split
#   # p n
#   # p n[0]
#   # n.split(",")
#   # p n
#   # p n[0]
#   p original_array
# end

# word_seperate("What even")

# 2. Create an array to store 4 letters. Then change the second letter to a number and print the array on one line.

# def two2(w)
#   array = [w]
#   array[0][1] = "2"
#   p array
# end

# two2("word")

# def two2(w)
#   array = w.split(//)
#   p array[1]
#   array[1] = 2
#   p array.class
#   p array
# end

# two2("word")

# 3. Create an array to store 5 numbers. Then print out each number on separate lines with a while loop.

# def explicit(w)
#   array = w.split("")
#   p array
#   array.each do |l|
#     p l
#   end
# end

# explicit("string")

# 4. Create an array to store 1 number. Then add three more numbers to the array and print the array on one line.

# def num(n)
#   array = [1]
#   array += n
#   p array
# end

# num([2, 3, 4])

# 5. Create an array to store 3 strings with lower case letters. Then change the third string to have all capital letters and print the array on one line.

# def three_upcase(array)
#   array[2] = array[2].upcase
#   p array
# end

# three_upcase(["first", "second", "third"])

# 6. Create an array to store 3 names. Then print out each name on separate lines with a while loop.

# def seperate(array)
#   array.each do |index|
#     p index.capitalize
#   end
# end

# seperate(["first", "second", "third"])

# 7. Create an array to store 2 strings. Then add one string to the array and print the array on one line.

# def add(array)
#   array << "third"
#   p array
# end

# add(["first", "second"])

# 8. Create an array to store 5 numbers. Then change the first number to 10 times its original value and print the array on one line.

# def pent(array)
#   array[0] = array[0] * 10
#   p array
# end

# pent([1, 2, 3, 4, 5])

# 9. Create an array to store 2 numbers. Then print out each number on separate lines with a while loop.

# def split(n)
#   n.each do |index|
#     p index
#   end
# end

# split([10, 20])

# 10. Create an array to store names of 3 different countries. Then add one more country and print the array one line.

# def country(array)
#   array << "Japan"
#   p array
# end

# country(["USA", "UK", "Canada"])

# SOLUTIONS: https://gist.github.com/peterxjang/7095a2b19e1da2cc18d4a0dcd66cb8f1
@sou7hernsaint
